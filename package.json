{
  "name": "akkc",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Full-stack practice work desk for A K Kataruka and Company.",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "dev": "sh -c 'trap \"kill 0\" INT TERM; npm run dev --workspace backend & npm run dev --workspace frontend -- --host 127.0.0.1 & wait'",
    "lint": "npm run lint --workspace backend && npm run lint --workspace frontend",
    "test": "npm test --workspace backend",
    "build": "npm run build --workspace frontend",
    "prisma:generate": "npm run prisma:generate --workspace backend",
    "prisma:migrate": "npm run prisma:migrate --workspace backend",
    "prisma:seed": "npm run prisma:seed --workspace backend",
    "prisma:validate": "npm run prisma:validate --workspace backend"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
